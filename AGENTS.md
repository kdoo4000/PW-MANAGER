# PWMANAGER 작업 기본 원칙

- Unity Editor 제어·컴파일·테스트·검증은 `unity-cli` 스킬과 Unity CLI만 사용한다. Unity MCP 도구와 `unity-mcp-orchestrator` 스킬은 사용하거나 다시 연결하지 않는다. CLI에 필요한 `com.unity.pipeline` 패키지는 유지한다.

- 코드 구조, 파일 관계, 데이터 흐름, 변경 영향 범위를 파악할 때는 `graphify`를 우선 사용한다. 기존 `graphify-out/graph.json`이 있으면 재생성하지 말고 먼저 조회한다.
- 구현과 수정에는 `ponytail:ponytail`의 `full` 원칙을 기본 적용한다. 실제 흐름을 확인한 뒤 기존 코드와 Unity 기본 기능을 재사용하고, 동작하는 가장 작고 단순한 변경을 선택한다.
- 코드를 작성, 검토, 디버깅하거나 리팩터링할 때는 `andrej-karpathy-skill` 원칙을 함께 적용한다. 구현에 영향을 주는 가정을 먼저 확인하고, 요청에 필요한 파일만 수술적으로 수정하며, 관찰 가능한 완료 조건과 가장 좁은 검증 방법을 정한다.
- 사용자 응답에는 `caveman`의 간결한 표현 원칙을 적용하되 한국어의 의미와 문법을 보존한다. 안전 경고, 되돌릴 수 없는 작업, 복잡한 순서 설명에서는 압축보다 명확성을 우선한다.
- `headroom`이 현재 세션에서 사용 가능한 스킬 또는 도구로 노출되면 기본 적용한다. 노출되지 않은 상태에서는 동작을 임의로 추측하지 않는다.
- 사용자가 별도로 요청하지 않는 한 화면에 디버그 문구, 내부 우선순위, 임시 안내 문구를 추가하지 않는다.
- 게임 화면에 표시하는 모든 선수 이름은 해당 선수 페이지로 연결한다. 단독 이름은 `WrestlerProfileController.CreateLink`, 문장 속 이름은 `WrestlerNameText`를 재사용하고 선수 ID로 이동한다.
- UI를 디자인할 때 제목 바로 아래에 한 줄 설명이나 안내 문구를 배치하지 않는다. 필요한 정보는 해당 조작 요소나 빈 상태에서만 직접 전달한다.
- UI 레이아웃은 주어진 화면과 컨테이너 영역을 최대한 활용한다. 고정 크기로 빈 공간을 남기지 말고 요소의 크기와 간격을 조절해 채우며, 특별한 의도가 없다면 상단과 하단 여백을 최대한 균등하게 맞춘다.
- 게임 UI의 기본 글꼴은 프로젝트에 포함된 세방고딕을 사용한다. 일반 텍스트와 중간 강조는 `SEBANGGothic-Regular.ttf`, 제목과 강한 강조는 `SEBANGGothic-Bold.ttf`를 사용한다.
